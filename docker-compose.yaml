version: '3'

services:
  db:
    image: postgres
  apptrack:
    image: apptrack
    build: .
    command: sh run.sh
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
